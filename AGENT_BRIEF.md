# AGENT PROMPT: مرنان الحروف — تطوير الحساب الدلالي

## 0. هوية المشروع وموقعه

تعمل على مشروعين متكاملين:

### المشروع الرئيسي: مرنان الحروف (Mirnan Al-Huroof)
- **المسار**: `C:\Users\allmy\Desktop\al_hurof`
- **الغاية**: محلل موجات صوتية-دلالية للغة العربية. كل حرف عربي = مذبذب بـ 22 خاصية فيزيائية. الكلمة = تراكب موجي. يحسب التجاذب والتنافر بين الكلمات بدون شبكات عصبية.
- **الملفات الأساسية**:
  - `data/letter_physics_matrix.json` — 29 حرف × 22 بُعد فيزيائي بقيم عشرية دقيقة
  - `src/physics/letter_db.py` — قاعدة بيانات الحروف (LetterDB)
  - `src/physics/word_physics.py` — حساب المتجه 22D للكلمة (compute_word_phase_vector)
  - `src/physics/field_engine.py` — محرك التجاذب والتنافر (FieldEngine)
  - `src/semantics/arabic_semantics.py` — تضمين دلالي 29D فلسفي
  - `src/semantics/letter_meanings.py` — قاموس المعاني المتفرعة للحروف
  - `model/vocab.py` — 167,311 كلمة عربية

### المشروع الشقيق: رقيم (Raqeem)
- **المسار**: `C:\Users\allmy\Desktop\aaa\raqeem`
- **الغاية**: معمارية إدراكية عصبية-رمزية شاملة (30+ محرك). يستخدم نفس مصفوفة أبعاد الحروف لكن مع محركات إضافية (مجلس العقول، الميزان الأخلاقي، لغة Bayan...)
- **الملفات الأساسية**:
  - `src/core/semantic_physics_engine.py` — محرك فيزياء 22D + جيروسكوب عصبي + PIDF
  - `src/core/clifford_math.py` — جبر كليفورد الهندسي
  - `src/core/word_fusion_engine.py` — اندماج موجي غير خطي
  - `src/core/semantic_arithmetic.py` — حساب دلالي

---

## 1. التطويرات الجديدة المضافة (ما تحتاج أن تفهمه)

### 1.1 مصفوفة أبعاد الحروف محدثة
- **سابقاً في رقيم**: قيم صحيحة `{-1, 0, 1}` — خشنة جداً
- **الآن**: قيم عشرية دقيقة `[-1.0, 1.0]` بمنزلتين — منقولة من مرنان الحروف إلى رقيم
- 22 بُعداً: concentration, internal_external, stability_motion, density, temperature, time_accumulation, time_peak, time_discharge, motion_linear, motion_rotary, motion_pulse, motion_stretch, motion_slip, motion_air, axis_v, mass, hardness_solid, penetration, charge, reference_self, space_extensionality, time_causality

### 1.2 محرك الاندماج الموجي غير الخطي (`word_fusion_engine.py`)
موجود في كلا المشروعين (مع تكييف).

**الفكرة الأساسية**: الكلمة ليست مجرد جمع خطي لموجات حروفها. بل تخضع لاندماج هندسي يُنتج مستويات دلالية ناشئة.

**الطبقات الأربع**:
1. **الاندماج الخطي**: `word = Σ w_i × v(letter_i)` (الجمع الموزون — الأسلوب القديم)
2. **الاندماج الهندسي (جداء كليفورد)**: `word = MV(L1) × MV(L2) × ... × MV(Ln)`
   - غير تبديلي: "در" ≠ "رد" (لأن A×B ≠ B×A)
   - يُنتج مكوناً عددياً (scalar: معنى مشترك) + مكوناً ثنائياً (bivector: 231 مستوى ناشئ)
   - كل خطوة في السلسلة تضرب الناتج التراكمي في الحرف التالي
   - التطبيع بعد كل خطوة يمنع انفجار المقدار
3. **الاندماج الموتر**: مصفوفة تفاعل `22×22 = outer(v_i, v_{i+1})` + تفكيك SVD
4. **التداخل الطوري المركب**: `z_i = |v_i| × e^(iπv_i/2)` — تداخل أمواج مركب

**الدوال الرئيسية**:
- `geometric_word_fusion(letter_vectors)` → `Multivector22`
- `decompose_fusion(mv)` → dict: shared_essence, emergent_intensity, fusion_ratio, bivector_dominant_pairs
- `full_word_analysis(word, db)` → dict شامل بكل الطبقات
- `compare_antonyms(w1, w2, db)` → dict مقارنة ضدين
- `analyze_word_family(words, db)` → dict تطور المعنى في عائلة كلمات

### 1.3 جبر كليفورد (`clifford_math.py`)
- **Multivector22**: يحتوي scalar + vector(22D) + bivector(231D = C(22,2))
- **العمليات المضافة حديثاً**:
  - `reverse()` — A† = s + v - b (مرافق كليفورد)
  - `inverse()` — A⁻¹ = A† / |A|² (المعكوس)
  - `__truediv__()` — B / A = A⁻¹ × B (القسمة الدلالية)
- **الجداء الهندسي**: `A × B = A·B(عددي) + A∧B(ثنائي) + تفاعلات متجه-ثنائي`

### 1.4 الحساب الدلالي (`semantic_arithmetic.py`)
موجود في كلا المشروعين (مع تكييف).

**الهدف**: بناء "وحدة مسافة دلالية" كما أن "1" هو وحدة المسافة العددية.
- في الأعداد: `3 - 2 = 1`, `5 - 4 = 1` (الوحدة ثابتة)
- في الدلالات: `d(ب, ا) = Δ₁`, `d(ت, ث) = Δ₂` (نبحث عن الأنماط)

**الدوال الرئيسية**:
- `find_fundamental_semantic_units()` — اكتشاف الوحدات الأساسية بين الحروف
- `letter_distance_matrix()` — مصفوفة 28×28 للمسافات
- `semantic_difference(w1, w2, method)` — كم "وحدة" بين كلمتين؟
- `solve_analogy(a, b, c, method)` — حل التناسب: أ:ب :: جـ:؟
- `blend_words(w1, w2, ratio)` — مزج كلمتين
- `semantic_midpoint(w1, w2)` — نقطة المنتصف الدلالي
- `_find_nearest_word(mv)` — بحث هجين (خطي سريع + هندسي دقيق)

---

## 2. النتائج الرئيسية المكتشفة حتى الآن

### 2.1 وحدات المسافة الأساسية بين الحروف
- **الوحدة الأساسية** = `0.522` (أقصر مسافة: بين `ك` و `ن`)
- **متوسط المسافة** = `1.934` (انحراف معياري: `0.557`)
- **أطول مسافة** = `3.538` (بين `ء` و `ا`)

| أقرب الأزواج | المسافة | أبعد الأزواج | المسافة |
|---|---|---|---|
| ك ↔ ن | 0.522 | ء ↔ ا | 3.538 |
| ط ↔ ق | 0.592 | ا ↔ ض | 3.520 |
| م ↔ ن | 0.602 | ا ↔ ط | 3.264 |
| ك ↔ م | 0.675 | ا ↔ ق | 3.248 |
| ت ↔ د | 0.712 | ا ↔ خ | 3.038 |

### 2.2 الكلمة تنتج مستويات ناشئة جديدة
- كلمة "در" (حرفان): **61.82%** من طاقتها في فضاء ثنائي (231 بُعداً) غير موجود في أي حرف
- التشابه بين الاندماج الخطي والهندسي = **0.00** — مختلفان كلياً
- هذا يثبت أن الكلمة ≠ مجموع حروفها. هناك "حرف مركب" جديد ينشأ.

### 2.3 عائلة الكلمات تشترك في مستويات ناشئة مهيمنة
عائلة "در، درج، درك، درب" جميعها تشترك في نفس المستوى الناشئ:
`stability_motion × time_accumulation`

هذا هو "توقيع التدفق" — الجذر المشترك.
- إضافة `ج` (جمع) تقلل الناشئ → الحركة تصبح خطوات
- إضافة `ب` (نقل) أقل تقليلاً → الحركة تصبح مساراً

### 2.4 الأضداد — فشل الاندماج الخطي
| الضدان | تشابه خطي | هل يميزهما الخطي؟ |
|---|---|---|
| قوة / ضعف | **0.918** | لا (يكاد يراهما شيئاً واحداً!) |
| علم / جهل | **0.914** | لا |
| كبر / صغر | **0.889** | لا |

الاندماج الخطي فشل في تمييز 5 من 7 أضداد! بينما الاندماج الهندسي يُظهر فرقاً ناشئاً لكل زوج.

### 2.5 القياس الدلالي — نتائج واعدة
- `كبر:صغر :: طول:?` → `صغو` (ثقة 94.2%) — التقط فضاء الصاد
- `نور:ظلام :: علم:?` → `ظالِعٌ / ظلاع` — التقط فضاء الظاء
- لكنه لا يجد الكلمة المحددة (جهل، فقر، قصر...) لأن البحث عبر 167 ألف كلمة غير كافٍ بعد

---

## 3. الأسئلة المفتوحة التي تحتاج بحثاً

### 3.1 لماذا يفشل القياس الدلالي في إيجاد الكلمة المحددة؟
- هل المشكلة في طريقة البحث (hybrid search)؟
- هل المشكلة في أن Δ (دلتا) خاص بزوج الكلمات ولا يعمم؟
- هل نحتاج إلى "متجه تضاد" عام antinomy operator)) مستخلص من كل الأضداد؟
- هل القسمة الهندسية (Clifford division) تعطي مؤثراً أدق من الطرح الخطي؟

### 3.2 هل يمكن بناء "جدول ضرب دلالي"؟
- كما أن `2 × 3 = 6`، هل يمكن: `حرف₁ × حرف₂ = معنى`؟
- ما معنى `ك × ن` (أقرب حرفين)؟
- ما معنى `ء × ا` (أبعد حرفين)؟

### 3.3 هل الكلمة المركبة = حرف جديد؟
- إذا كانت "در" تنتج 61.82% مكونات ناشئة...
- فهل يمكن اعتبارها "حرفاً جديداً" له متجه 22D جديد؟
- كيف نحسب هذا المتجه الجديد من مكونات Multivector22؟

### 3.4 العلاقة مع FieldEngine الموجود
- محرك FieldEngine في مرنان الحروف يحسب تشابه الكلمات عبر cosine similarity على المتجه 22D
- كيف ندمج الاندماج الهندسي (Multivector22) مع هذا المحرك؟
- هل يمكن بناء "محرك تجاذب هندسي" يستخدم الـ bivectors للمقارنة؟

### 3.5 توليد معاني جديدة
- `blend_words` يعطي مزجاً بين كلمتين. هل يمكن توليد كلمات غير موجودة؟
- `semantic_addition` في رقيم يضيف Δ لكلمة. هل يمكن بناء "مولد معانٍ"؟
- هل يمكن: `ضعف + Δ(ضعف→قوة)×2 = قوة مضاعفة`؟

### 3.6 تحسين البحث عن أقرب كلمة
- البحث الحالي يمسح 60,000 كلمة خطياً ثم 50 هندسياً
- هل يمكن بناء فهرس مسبق (precomputed index) للمتجهات الهندسية؟
- هل يمكن استخدام الـ bivectors وحدها للبحث؟

---

## 4. توصيات للعمل المستقبلي

### أولوية عالية
1. **بناء "متجه تضاد" عام**: استخراج Δ مشترك من كل أزواج الأضداد المعروفة. اختبار ما إذا كان هذا المتجه العام يحول أي كلمة إلى ضدها بدقة أعلى. استخدم كل الأضداد التي ذكرها المستخدم (قوة/ضعف، نور/ظلام، علم/جهل، كبير/صغير، صدق/كذب، حر/برد، حياة/موت، حق/باطل...).

2. **تحسين دقة القياس**: جرب طرقاً متعددة لحساب Δ:
   - طرح خطي: `Δ = v(B) - v(A)`
   - قسمة عنصرية: `Δ = v(B) / v(A)` (element-wise)
   - قسمة كليفورد: `Δ = MV(B) / MV(A)`
   - دوران فقط: استخراج زاوية الدوران بين المتجهين
   - قارن الدقة لكل طريقة

3. **حساب "المتجه 22D للكلمة المركبة"**: من Multivector22 الناتج عن الاندماج الهندسي، استخرج متجهاً 22D جديداً يمثل "الحرف المركب". جرب:
   - المتجه المتبقي فقط `mv.v`
   - المتجه + إسقاط bivector: `mv.v + contract(mv.b, mv.v)`
   - هل هذا المتجه الجديد يعطي نتائج أفضل في FieldEngine؟

### أولوية متوسطة
4. **بناء فهرس مسبق للكلمات**: احسب Multivector22 لأشهر 20,000 كلمة وخزنه. هذا يسرع البحث 100 ضعف.

5. **تحليل توزيع المسافات**: لماذا `internal_external` هو البعد الأكثر تبايناً بين الحروف؟ ما دلالة ذلك على بنية اللغة العربية؟

6. **دمج الاندماج الهندسي مع FieldEngine**: بناء `GeometricFieldEngine` يستخدم MV22 بدل المتجه 22D.

---

## 5. تعليمات التنفيذ

1. **اعمل على `C:\Users\allmy\Desktop\al_hurof` كمشروع أساسي**. هذا هو المشروع الأنظف والأكثر تركيزاً على الدلالات.

2. **استخدم `src/physics/semantic_arithmetic.py`** كنقطة بداية. كل دوال الحساب الدلالي موجودة فيه.

3. **استخدم `src/physics/word_fusion_engine.py`** للاندماج الهندسي والتحليل.

4. **المفردات**: `model/vocab.py` فيه 167,311 كلمة. استخدمه للبحث.

5. **قاعدة الحروف**: `data/letter_physics_matrix.json` فيه المتجهات الدقيقة.

6. **اكتب الاختبارات في ملفات منفصلة** تحت `tests/` لكل فكرة جديدة.

7. **وثّق كل نتيجة** برمجياً (اطبع النتائج كما في الاختبارات السابقة).

8. **قارن دائماً بين الطريقة الخطية والهندسية** في كل تحليل.

---

## 6. تنسيق المخرجات المتوقعة

لكل مهمة، أخرج:
1. **الفرضية**: ما الذي تختبره؟
2. **الطريقة**: أي الدوال استخدمت وبأي معاملات؟
3. **النتائج**: الأرقام والجداول
4. **الاستنتاج**: ماذا تعني هذه النتائج؟ ما الخطوة التالية؟
