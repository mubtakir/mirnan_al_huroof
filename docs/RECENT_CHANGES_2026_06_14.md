# سجل تغييرات 2026-06-14

هذا سجل مختصر للتغييرات التي تمت بعد مراجعة `src/physics/al_aql` وبقايا لغة بيان القديمة.

## ميزان التكرار الوهمي في التدريب

أضيفت طبقة موازنة خفية لمعالجة طغيان الكلمات والموضوعات كثيرة الورود أثناء بناء مصفوفات الاقتران.

التغييرات:

- أضيفت وحدة `src/physics/engines/training_balance.jl`.
- صار `train.jl` يحسب تكرار كل كلمة مرة واحدة بعد بناء المعجم.
- يبني التدريب أوزاناً ناعمة تخفض أثر الكلمات الطاغية وترفع أثر الكلمات الأقل تمثيلاً ضمن سقف محدود.
- تستعمل الأوزان عند بناء `K_sem` و`K_syn` و`K_dialogue` و`K_causal` دون إضافتها إلى الكوربس كنص.
- يحفظ التدريب تقريراً في `model/word_balance_weights.json`.
- يمكن تعطيلها مؤقتاً للمقارنة عبر `--no-balance`.

الغرض:

- تقليل انحياز المصفوفات نحو الموضوعات الأكثر حضوراً.
- منع إنشاء سياقات وهمية كانت ستنتج عن إضافة مستندات مصطنعة.
- إبقاء الموازنة أداة تدريب مخفية لا مادة لغوية للتوليد.

## إزالة مسار بيان القديم

تم التأكد أن لغة بيان الهجينة القديمة لم تعد هي المسار الصحيح لـ `al_aql`.

التغييرات:

- أزيل تحميل `bayan_analyzer.jl` من `src/physics/Physics.jl`.
- حذف ملف `src/physics/engines/bayan_analyzer.jl`.
- حذف الاختبار القديم `test_bayan_analyzer.jl`.
- بقيت بعض الوثائق القديمة التي تذكر بيان كأثر أرشيفي، ولا تمثل مسار التشغيل الحالي.

المسار المعتمد الآن:

```text
al_aql ADL + IdeaEngine
```

## تفعيل IdeaEngine

تم وصل `src/physics/engines/idea_engine.jl` داخل `Physics.jl`.

الرموز المصدرة:

```julia
Idea
compute_idea_vector
match_sentence_to_idea
score_candidate_for_idea
```

الغرض:

- تمثيل الفكرة كموجة دلالية مكونة من أشياء وحدث ونتيجة.
- مطابقة جملة مع فكرة.
- تقييم كلمة مرشحة ضمن سياق فكرة.

## توسيع Al-Aql

أضيفت طبقة كيان/فعل/قاعدة عامة إلى `al_aql`.

الرموز الجديدة:

```julia
PropertyEffect
DynamicVerb
CausalRule
register_verb!
add_rule!
apply_verb!
evaluate_idea!
```

الغرض:

- تعريف أفعال جديدة أثناء التشغيل دون كتابة دالة Julia جديدة لكل فعل.
- تعريف قواعد سببية مثل: إذا زاد الخوف فشغل الهرب.
- تمكين ADL من بناء فضاء محاكاة كامل.

## إضافة ADL

أضيف الملف:

```text
src/physics/al_aql/dsl.jl
```

الدوال الجديدة:

```julia
compile_adl!
train_from_text!
property_key
```

يدعم `compile_adl!`:

- تعريف كيان.
- تعريف فعل.
- تعريف قاعدة.
- تنفيذ فكرة.

يدعم `train_from_text!` استخلاصا أوليا من النص العربي، مثل:

```text
زأر الأسد على الغزال يرفع الخوف.
إذا زاد الخوف عن 0.5 يهرب الغزال.
الأسد زأر على الغزال.
```

## إصلاح CausalFlow

كانت تظهر تحذيرات من اختلاف أبعاد المتجهات:

```text
DimensionMismatch: 10000 vs 64
```

السبب:

- `CausalFlowField` يعمل غالبا على `dim=64`.
- المولد يمرر أحيانا متجهات كاملة بطول `10000`.
- عند استخدام المتجه الحالي كهدف احتياطي، كان `CausalFlow` يطرح متجها كاملا من متجه مختزل.

الإصلاح:

- أضيفت دالة داخلية لإسقاط أي متجه إلى `cf.dim`.
- صار `compute_flow` و`flow_alignment_score` يستخدمان الإسقاط قبل الجمع والطرح.

الملف:

```text
src/physics/engines/causal_flow.jl
```

## ربط التدريب بالرسائل

أضيفت بعد وثيقة سير التعلم والرسائل مجموعة وصلات عملية:

- صار `MirnanGenerator` يقبل `K_causal` ويستخدمها في `CausalFlow`، مع الرجوع إلى `K_sem` عند غيابها.
- صار `MirnanGenerator` يحمل `K_code` تلقائيا من `model/code_vocab.json` و`model/K_code.dat` عند وجودهما.
- أصلحت صيغة حفظ/تحميل `K_code` إلى `SPARSE_COO_V1`.
- صار الخادم الداخلي `src/api/server.jl` يحاول تحميل النموذج المدرب من `model/` قبل الرجوع إلى مولد افتراضي صغير.
- صار `generate!` يبني طبقة توجيه عقلية من `al_aql` قبل التوليد العام.
- أضيفت قناة `aql_guidance` داخل `_score` حتى تسهم العلاقات والقوالب والعمليات والغايات والاستثناءات في اختيار الكلمات.
- صار `generate!` يجيب عبر `al_aql` عن العلاقات والظروف والأحداث والعمليات والمجاز والنية والاستثناء والمقارنة والكم ووصف الكيان عند توفر معرفة صريحة.
- أضيف حفظ/تحميل أولي للتعلم اللحظي عبر:

```julia
save_runtime_learning!(gen)
load_runtime_learning!(gen)
```

ويحفظ افتراضيا في:

```text
model/runtime_learning/runtime_learning.json
```

## تثبيت التوليد بعد التدريب الكبير

بعد تدريب نموذج بحجم 200000 كلمة ومصفوفات تتجاوز 100 مليون اقتران، ظهر أن عينة التوليد الرنيني تنجرف إلى أدوات وكلمات corpus عالية الانتشار.

لذلك عُدل مسار `resonant` في `generator.jl`:

- أضيف فلتر مرشحين يمنع الرموز والكلمات الوظيفية القصيرة والضوضاء الشكلية.
- صار المسار الرنيني يختار أعلى مرشح رنيني بدلا من اختيار عشوائي من softmax.
- بقيت كلمات prompt مرساة نشطة أثناء التوليد حتى لا تنجرف الجملة بسرعة.
- دخلت `K_causal` في التنشيط الرنيني بجانب `K_sem`.
- صار `aql_guidance` مؤثرا في المسار الرنيني أيضا، لا في `_score` القياسي فقط.
- خُفف أثر `K_syn` داخل الرنيني الكبير لأنه كان يجذب التوليد إلى انتقالات شكلية.

## تدريب الكود وذاكرة al_aql

أضيفت وصلتان جديدتان إلى `train.jl`:

- صار التدريب يلتقط ملفات الكود الخام قبل فلتر العربية، مثل ملفات `code_corpus` و`.py.txt` و`.jl.txt`، ويبني منها `K_code`.
- صار التدريب ينتقي جملا سببية مرشحة من corpus ويدخلها إلى `al_aql` ثم يحفظها في `model/runtime_learning/runtime_learning.json`.

كما صار `train_from_text!` في `al_aql` يتعرف على نمط مثل:

```text
إضافة كمية من ملح إلى الماء فإنها تزيد من درجة غليانه
```

ويحوله إلى قالب سببي: `إضافة ملح إلى ماء -> زيادة درجة الغليان`.

## حوكمة المعرفة والكبح العقلي

أضيفت طبقة حوكمة داخل `SimulationSpace`:

```julia
AqlGovernanceRule
RejectionLesson
CorpusAnnotation
```

وصارت `al_aql` تميز بين:

- `curated_rules`: قواعد موثوقة لها أولوية عليا.
- `proposed_rules`: قواعد مرشحة للمراجعة.
- `rejected_rules`: قواعد مرفوضة تحفظ حتى لا تعود كاقتراح قوي.
- `rejection_lessons`: دروس عامة من أسباب الرفض.
- `corpus_annotations`: وسوم نقدية على corpus.

أضيفت دوال:

```julia
curate_rule!
propose_rule!
approve_rule!
reject_rule!
learn_rejection_lesson!
critical_corpus_pass!
aql_memory_audit
annotation_weight
aql_inhibition_score
```

ودخل `aql_inhibition` في `_score` كموجة هدّامة، بحيث يستطيع العقل كبح مرشح مرفوض أو مخالف لقاعدة موثوقة، لا مجرد تقوية مرشح آخر.

كما صارت هذه الطبقة تحفظ وتحمل ضمن `runtime_learning.json`.

## مرصد فيزياء الكلمة

أضيفت طبقة جديدة فوق فيزياء الحرف:

```text
src/physics/engines/lexical_oracle.jl
```

الدوال الجديدة:

```julia
word_physics_profile
phonosemantic_quality
nearest_phase_words
analyze_unknown_word
coin_word_for_concept
```

الغرض:

- تحليل كلمة جديدة من حروفها قبل الاعتماد على corpus.
- حساب أقرب كلمات طيفيا في المعجم.
- إعطاء مؤشرات: التناغم، الثبات، الثقل، والجمال الطيفي.
- توليد ألفاظ مرشحة لمفهوم من جذور كلمات seed.
- تمكين `generate!` من جواب مباشر لصيغة مثل: `حلل كلمة نوارية`.

## الاختبارات

آخر تحقق بعد هذه التغييرات:

```text
test/runtests.jl       -> 298 / 298 passed
test/run_all_tests.jl  -> 18 / 18 passed
```

كما اختفت تحذيرات `CausalFlow` أثناء `test/runtests.jl`.
