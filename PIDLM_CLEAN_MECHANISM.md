# مِيكانِيكا مِرنان النظيف: حقل الجذب والتنافر الفيزيائي ⚛️🌊

يقوم هذا المستند بتوثيق الأساس الفيزيائي والرياضي لنسخة **مرنان الحروف النظيف** (المبني على فضاء المتجهات الطورية لحروف الكلمة).

---

## 1. الفلسفة الفيزيائية والروح اللغوية

النموذج مبني على فرضية أساسية: **اللغة العربية لغة توقيفية ذات أبعاد فيزيائية طبيعية**.
* **الحرف**: ليس مجرد رمز مجرد، بل هو **مذبذب موجي (Wave Oscillator)** له تردد ذاتي وخصائص مادية (صوتية وأثيرية) وخصائص فلسفية (تكوينية).
* **الكلمة**: هي **حزمة موجية (Wave Packet)** ناتجة عن تداخل موجات الحروف المكونة لها.
* **المجال الدلالي**: هو تداخل وتوافق الأطوار (Phase Alignment) في فضاء متعدد الأبعاد. الكلمات ذات الأبعاد المتقاربة تتجاذب طبيعياً، بينما تتباعد الكلمات ذات الأبعاد المتنافرة، دون الحاجة لتدريب إحصائي تقليدي أو أوزان عصبية اصطناعية.

---

## 2. الفضاءات الطورية والأبعاد الثلاثة

يتم تمثيل كل كلمة في المعجم بمتجه طوري مدمج يتكون من **38 بُعداً**، مقسمة إلى فضاءين أساسيين:

### أ. الطور المادي الأصواتي (Physical Phase - 22D)
يمثل الخصائص الفيزيائية ومخارج الحروف وسماتها الصوتية في اللسان العربي، ويحتوي على الأبعاد التالية:
1. `concentration` (التركيز)
2. `internal_external` (الداخل والخارج)
3. `stability_motion` (الاستقرار والحركة)
4. `density` (الكثافة)
5. `temperature` (الحرارة)
6. `time_accumulation` (التراكم الزمني)
7. `time_peak` (الذروة الزمنية)
8. `time_discharge` (التفريغ الزمني)
9. `motion_linear` (الحركة الخطية)
10. `motion_rotary` (الحركة الدائرية)
11. `motion_pulse` (الحركة النبضية)
12. `motion_stretch` (الحركة الامتدادية)
13. `motion_slip` (الحركة الانزلاقية)
14. `motion_air` (الحركة الهوائية)
15. `axis_v` (المحور العمودي)
16. `mass` (الكتلة الموجية)
17. `hardness_solid` (الصلابة)
18. `penetration` (النفاذية)
19. `charge` (الشحنة)
20. `reference_self` (المرجعية الذاتية)
21. `space_extensionality` (الامتداد الفضائي)
22. `time_causality` (السببية الزمنية)

### ب. الطور الفلسفي الدلالي (Philosophical Phase - 16D)
يمثل البعد الدلالي التكويني لكل حرف كما تم استنباطه من المعاني الجذرية للحروف العربية:
* `أ` = ابتداء / مصدر وأصل.
* `ب` = بناء / تأسيس واحتواء.
* `ح` = حياة / طاقة وحركة ذاتية.
* `ع` = اقتلاع / إظهار وقوة عارضة.
* `ي` = عمق / امتداد وتأثير ممتد.
* وباقي الحروف كما هي معرّفة في كلاس `CharacterSemanticEmbedding` لإنتاج موجات جيبية متباينة التردد ($\sin$ / $\cos$) تصنع بصمة طورية مميزة لكل فئة دلالية.

---

## 3. الصياغة الرياضية وحساب المتجهات

### حساب متجه الكلمة الطوري
يتم حساب البصمة الفيزيائية للكلمة $v_{\text{word}}$ بدمج متجهات حروفها الفردية مع إعطاء وزن مضاعف للحرف الأول والأخير (لكونهما ركيزتي المعنى وصورة البدء والختام للكلمة)، وإدخال تفاعل أزواج الحروف المتجاورة (Pairwise Interaction) بنسبة معامل ثقل $\gamma = 0.3$:

$$weighted = \frac{1}{\sum w_i} \sum_{i=1}^{n} w_i v_{\text{letter}_i}$$
$$\text{where } w_i = \begin{cases} 2.0 & \text{if } i = 1 \text{ or } i = n \\ 1.0 & \text{otherwise} \end{cases}$$

$$\text{Pairwise interaction: } v_{\text{pairs}} = \frac{1}{n-1} \sum_{i=1}^{n-1} v_{\text{letter}_i} \cdot v_{\text{letter}_{i+1}}$$

$$v_{\text{word}} = \text{Normalize}(weighted + \gamma v_{\text{pairs}})$$

### قياس المجال الطوري (الجذب والتنافر)
لقياس شدة التجاذب أو التنافر بين الكلمة المستهدفة $v_t$ وكلمات المعجم $v_w$، يتم استخدام **تشابه جيب التمام الموجه (Cosine Similarity)**:

$$\text{Similarity}(v_t, v_w) = \frac{v_t \cdot v_w}{\|v_t\| \|v_w\|}$$

### جـ. التعزيز الدلالي الهيبي (Hebbian Semantic Boost)
لدمج العلاقات المكتسبة من نصوص التدريب (السياق والارتباط العام)، يتم تعزيز تشابه جيب التمام المجهر بمصفوفة الاقتران الدلالي $K_{\text{sem}}$ (إن وُجدت):
$$\text{Score}(w_j) = \text{Similarity}(v_t, v_j) \times (1.0 + 3.0 \times K_{\text{sem}}[t, j])$$

* **التجاذب (Attraction)**: تقارب طوري معزز بالارتباط السياقي يرتفع دلالياً إلى قيم موجبة عالية.
* **التنافر (Repulsion)**: تباعد طوري أو خلو من الارتباط السياقي يقترب قطبياً من القيم السالبة.

---

## 4. تصفية وتطبيع المخرجات

تعتمد الفلاتر والخوارزميات البرمجية على استبعاد التطابقات الميكانيكية لضمان جودة ونقاء المعاني المستخرجة:
1. **تجريد التشكيل (Diacritic Stripping)**: يتم إزالة الحركات الإعرابية والتزيين (الفتحة، الضمة، الكسرة، الشدة، التنوين...) أثناء الفرز لمنع تكرار نفس الكلمة بأشكال إعرابية مختلفة في القائمة الواحدة.
2. **استبعاد الكلمات المشتقة داخلياً**: يتم استبعاد أي كلمة تحتوي على الكلمة المستهدفة كجزء منها أو العكس (مثلاً: عند فحص كلمة "علم" نستبعد "معلم" و "العلوم" و "تعليم" تلقائياً) لضمان ظهور الكلمات ذات الصلة الدلالية الحرة (مثل ظهور "عمل" أو "عجم" أو "نور" نتيجة تشابه بنية الحروف والأطوار الحيوية).

---

## 5. الهيكل البرمجي النظيف للمشروع

تم تصفية المشروع وحذف كافة المكونات المعقدة الإضافية، ليقتصر على الملفات التالية فقط:

```
mirnan_al_huroof0/
├── data/
│   └── letter_physics_matrix.json     # مصفوفة الخصائص الصوتية والمادية للحروف (22 بُعداً)
├── model/
│   ├── __init__.py                    # واجهة تحميل معجم الكلمات
│   └── vocab.py                       # معجم الكلمات الضخم (167,311 كلمة عربية)
├── src/
│   ├── api/
│   │   ├── __init__.py
│   │   └── main.py                    # خادم الويب المبسط (FastAPI) لخدمة حقل الجذب والتنافر
│   ├── physics/
│   │   ├── __init__.py                # تصدير الدوال والمحركات الرئيسية
│   │   ├── constants.py               # ثوابت الأبعاد الفيزيائية (22D)
│   │   ├── letter_db.py               # قارئ ومحمل مصفوفة الحروف
│   │   ├── word_physics.py            # حساب البصمة الفيزيائية الصوتية للكلمات
│   │   └── field_engine.py            # محرك الجذب والتنافر المتجهي والفرز ثلاثي الفضاءات
│   └── semantics/
│       ├── __init__.py
│       └── arabic_semantics.py        # حساب التضمين الفلسفي للحروف (16D)
├── ui/                                # ملفات واجهة الويب الرسومية
│   ├── index.html                     # واجهة العرض side-by-side
│   ├── style.css                      # تنسيق بصري غامر بتصميم Glassmorphic داكن
│   └── script.js                      # المنطق التفاعلي وعرض قوى الجاذبية
├── tests/
│   └── test_word_physics.py           # اختبارات الوحدة الأوتوماتيكية للتأكد من صحة الحسابات
├── cli.py                             # واجهة الأوامر التفاعلية في الطرفية
├── run.bat                            # مشغل النظام المباشر لويندوز
└── requirements.txt                   # متطلبات التشغيل الأساسية (NumPy و FastAPI فقط)
```

---

## 6. إرشادات التشغيل الفوري

* **لتشغيل الواجهة الرسومية البصرية**:
  شغل `run.bat` واختر الرقم **2**، ثم توجه للمتصفح: [http://127.0.0.1:8000](http://127.0.0.1:8000).
* **لتشغيل كونسول الطرفية التفاعلي**:
  شغل `run.bat` واختر الرقم **1**، أو اكتب: `python cli.py -i`.
* **لتشغيل الفحوصات الرياضية**:
  شغل `run.bat` واختر الرقم **3**، أو اكتب: `python -m pytest tests/test_word_physics.py -v`.
