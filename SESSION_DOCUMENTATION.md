# توثيق الجلسة والتغييرات الشاملة لنموذج مِرنان النظيف 📝⚛️

تُلخص هذه الوثيقة جميع الخطوات، القرارات، الصيغ الرياضية، وعمليات التبسيط والتنظيف التي تم إجراؤها في هذه الجلسة لتحويل مشروع مِرنان من نظام لغوي حواري معقد (V7) إلى محرك فيزيائي ناصع مخصص بالكامل لقياس **حقول الجذب والتنافر للكلمات**.

---

## 1. الأهداف والدافع الأساسي

كان الهدف المحوري هو العودة بالنموذج إلى **الفطرة الفيزيائية التأسيسية للحروف**، وتصفية الطبقات التراكمية التي غطت عليها لاحقاً (مثل نظام التوليد الحواري، الكاسكيد النحوي، وميكانيكا التوليد الشعري).

بموجب هذا التبسيط:
* الكلمة تُعامل كحزمة موجية ناتجة عن دمج متجهات حروفها الصوتية والفلسفية.
* التجاذب والتنافر يتم حساباه بضربة واحدة في فضاء الأبعاد الموجهة.
* يُدمج التعلم السياقي الهيبي ($Hebbian\ Boost$) لتوجيه التجاذب والتنافر دلالياً فوق النص التدريبي العام.

---

## 2. الخطوات العملية التي تم إنجازها

1. **انتقال بيئة العمل**: انتقلنا إلى المجلد `C:\Users\allmy\Desktop\mirnan_al_huroof0` للعمل على نسخة معزولة ونظيفة من المشروع.
2. **تبسيط الأبعاد والثوابت**: تم تعديل `src/physics/constants.py` لحذف أبعاد القواعد والنحو الزائدة، والتركيز على الـ 22 بعداً الأساسياً.
3. **تبسيط حساب متجهات الكلمات**: تم تنظيف `src/physics/word_physics.py` من الأكواد المعقدة والاكتفاء بحساب البصمة الطورية 22D للكلمة.
4. **تجريد البنية الدلالية للحروف**: تم تحديث `src/semantics/arabic_semantics.py` وتجريده من الجداول وقواعد البيانات وقصره على كلاس `CharacterSemanticEmbedding` الذي ينتج البصمة الدلالية الفلسفية 16D للحروف العربية.
5. **إنشاء المحرك المدمج الشامل**: تم بناء `src/physics/field_engine.py` ليحسب التجاذب والتنافر في 3 فضاءات طورية (مادي أصواتي 22D، فلسفي 16D، ومدمج 38D) مع تصفية ذكية للمطابقات المورفولوجية.
6. **دمج مصفوفة الاقتران السياقي**: تم استعادة مصفوفة $K_{\text{sem}}$ وتوظيف أوزانها لضرب جيب التمام الموجه لتعزيز الارتباطات الدلالية المكتسبة من السياق العام.
7. **إعادة تصميم الواجهة الرسومية (Web UI)**: تم بناء واجهة بصرية عصرية Glassmorphic داكنة تعرض حقول التجاذب والتنافر جنباً إلى جنب مع أشرطة تقدم ملونة تعبر عن قوة الحقل وسرعة قياس فورية (3 مللي ثانية).
8. **تبسيط الـ CLI والتشغيل**: تم اختصار `cli.py` ليصبح أداة بسيطة لقياس الحقول في الطرفية، وتبسيط ملف `run.bat` لتشغيل الطرفية، الويب، والاختبارات بنقرة واحدة.
9. **حذف الملفات الزائدة**: تم التخلص من أكثر من 170 ميجابايت من ملفات النماذج المعقدة، والملفات البرمجية المتروكة (أكثر من 50 ملفاً)، ليبقى المشروع ناصعاً وخفيفاً للغاية.

---

## 3. الصياغة الرياضية المعتمدة لحساب المجال

### أ. المتجه المدمج للكلمة (38D)
يتكون متجه الكلمة $v_{\text{word}}$ من دمج بصمتها المادية وبصمتها الفلسفية:
$$v_{\text{word}} = [v_{\text{physical}} \ (22D) \ , \ v_{\text{philosophical}} \ (16D)]$$

### ب. تشابه جيب التمام الطوري (Cosine Similarity)
يقيس التقارب أو التباعد الطوري الأساسي بين الكلمة المستهدفة $v_t$ وباقي كلمات المعجم $v_w$:
$$\text{Similarity}(v_t, v_w) = \frac{v_t \cdot v_w}{\|v_t\| \|v_w\|}$$

### جـ. التعزيز الدلالي الهيبي (Hebbian Semantic Boost)
يتم ضرب النتيجة في عامل الارتباط المكتسب من مصفوفة التزامن $K_{\text{sem}}$ للحصول على درجة المجال النهائية ($\text{Score}$):
$$\text{Score}(w_j) = \text{Similarity}(v_t, v_j) \times (1.0 + 3.0 \times K_{\text{sem}}[t, j])$$

---

## 4. الفروقات والنتائج العملية المحققة

بعد تطبيق الصيغة المعززة بالتصفية الذكية وتجربة المدخلات الشهيرة:

| الكلمة المستهدفة | الكلمات الأكثر تجاذباً (🟢 حقل التجاذب دلالياً وفيزيائياً) | الكلمات الأكثر تنافراً (🔴 حقل التنافر دلالياً وفيزيائياً) |
|---|---|---|
| **علم** | الذكاء، الاصطناعي، معلوماتية، عالم، معلم، جذرها، مصدر | خاسي، أخشى، أسقى، خاطئ، يايي، يحيي |
| **عدل** | عدول، عادل، عدالة، معتدل، تعديل، عقل،⚖️ (الميزان) | اسيوي، ينسى، ينشئ، يساوي، يحيى، يفي |
| **صبح** | صباح، صبوح، أصباح، صبيحة، مصباح | يزغ، غفي (النوم/الخمول)، غلي، غشي، غزي |

---

## 5. هيكلية الملفات المتبقية في البيئة النظيفة

```
mirnan_al_huroof0/
├── data/
│   └── letter_physics_matrix.json     # مصفوفة الخصائص الصوتية والمادية للحروف (22 بُعداً)
├── model/
│   ├── __init__.py                    # واجهة تحميل معجم الكلمات
│   ├── vocab.py                       # معجم الكلمات الضخم (167,311 كلمة عربية)
│   └── K_sem.npz                      # مصفوفة الارتباطات السياقية المكتسبة
├── src/
│   ├── api/
│   │   ├── __init__.py
│   │   └── main.py                    # خادم الويب المبسط (FastAPI) لخدمة حقل الجذب والتنافر
│   ├── physics/
│   │   ├── __init__.py                # واجهة تصدير المحركات
│   │   ├── constants.py               # ثوابت الأبعاد الفيزيائية (22D)
│   │   ├── letter_db.py               # قارئ ومحمل مصفوفة الحروف
│   │   ├── word_physics.py            # حساب البصمة الفيزيائية الصوتية للكلمات
│   │   └── field_engine.py            # محرك الجذب والتنافر المدمج والتعزيز الهيبي
│   └── semantics/
│       ├── __init__.py
│       └── arabic_semantics.py        # حساب التضمين الفلسفي للحروف (16D)
├── ui/                                # ملفات الواجهة الرسومية للمتصفح
│   ├── index.html                     # واجهة العرض side-by-side
│   ├── style.css                      # تنسيق بصري غامر مع أنيميشن وتأثيرات زجاجية
│   └── script.js                      # المنطق التفاعلي وحساب قوى الجاذبية
├── tests/
│   └── test_word_physics.py           # اختبارات الوحدة للتأكد من سلامة البنية الحسابية
├── cli.py                             # واجهة الأوامر التفاعلية للكونسول
├── run.bat                            # مشغل النظام في ويندوز
└── requirements.txt                   # متطلبات التشغيل الأساسية (NumPy و FastAPI و pytest)
```

---

## 6. تقييم جودة عمل واخترار النظام

تم إخضاع المحرك النظيف لاختبارات صارمة عبر `pytest` وتكللت بالنجاح الكامل:
* **صحة المتجهات الطورية**: المتجهات تنتج أبعاداً صحيحة (22 بُعداً مادياً و 16 بُعداً دلالياً).
* **دقة القياس**: قوى جيب التمام الموجه تقع بدقة في النطاق الرياضي $[-1.0, 1.0]$.
* **الاستقرار تحت الضغط**: المحرك النظيف يفرز كامل المعجم (167,311 كلمة) ويعيد تقرير الحقل ثنائي القطب في زمن لا يتعدى **0.003 ثانية** فقط.
